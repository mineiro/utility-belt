# Package Notes

Use this directory for one RPM package only.

Contents:

- `<name>.spec`
- `patches/` for patch files referenced by the spec
- `sources/` for local source artifacts only when needed
- `package.env` for metadata used by helper scripts
- `Makefile` for local SRPM generation
