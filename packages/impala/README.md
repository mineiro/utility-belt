# impala

RPM packaging directory for `impala`.

Template: `rust`

Upstream:

- Git: https://github.com/pythops/impala.git
- Releases: https://github.com/pythops/impala/releases

Notes:

- Rust package with vendored crates in `Source1`.
- Runtime expects `iwd` to be installed and running.
