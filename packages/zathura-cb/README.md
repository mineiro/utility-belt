# zathura-cb

RPM packaging directory for `zathura-cb` 2026.02.03.

Template: `generic`

Upstream:

- Git: https://git.pwmt.org/pwmt/zathura-cb.git
- Releases: https://pwmt.org/projects/zathura-cb/download/

Notes:

- Follows Fedora's existing standalone plugin package split.
- Builds the `libcb.so` backend for comic book archive support in the
  date-based `zathura` series.
